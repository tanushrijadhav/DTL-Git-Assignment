# DTL-Git-Assignment
This repository contains simple Java utility programs:  StringUtils.java → string operations (reverse, uppercase) NumberUtils.java → number operations (factorial, prime check)  These files are intentionally simple to clearly demonstrate Git operations rather than complex logic.
